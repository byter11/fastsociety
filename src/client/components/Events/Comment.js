import React from 'react'

const Comment = ({ data }) => {

    const { id, textContent, createdOn, user } = data;
    
    return (
        <div>
            helo
        </div>
    )
}

export default Comment;
